{
  "name": "@gc360/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:cloudflare": "wrangler dev --env dev",
    "build": "wrangler deploy --dry-run",
    "build:dev": "wrangler deploy --dry-run --env dev",
    "typecheck": "tsc --noEmit --pretty false",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "drizzle-orm": "^0.44.5",
    "hono": "^4.9.6",
    "jose": "^6.1.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "typescript": "^5.9.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.34.0"
  }
}
